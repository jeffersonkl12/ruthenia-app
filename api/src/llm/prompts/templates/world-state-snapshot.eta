# Estado Atual do Mundo
<% if (it.kingdom) { %>

## Reino
- Nome: <%= it.kingdom.name %>

- Status: <%= it.kingdom.status %>

<% } %>
<% if (it.region) { %>

## Região
- Nome: <%= it.region.name %>

<% if (it.region.description) { %>
- Descrição: <%= it.region.description %>

<% } %>
- Bioma: <%= it.region.biome %>

- Status: <%= it.region.status %>

- Riqueza: <%= it.region.wealth %>

- Infraestrutura: <%= it.region.infrastructure %>

<% } %>
<% if (it.location) { %>

## Localização
- Nome: <%= it.location.name %>

- Tipo: <%= it.location.type %>

<% if (it.location.description) { %>
- Descrição: <%= it.location.description %>

<% } %>
<% } %>
