# Segredos do Mestre (NÃO revelar ao jogador)
Use estas informações só para guiar decisões internas de narrativa — nunca as
revele diretamente ao jogador, a menos que ele descubra isso através de ações
no jogo.
<% if (it.notes) { %>

## Notas
<%= it.notes %>

<% } %>
<% if (it.secrets && it.secrets.length) { %>

## Segredos
<% for (const s of it.secrets) { %>
- <%= s.label %>: <%= s.detail %>

<% } %>

<% } %>
